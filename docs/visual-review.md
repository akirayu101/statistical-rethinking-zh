# 视觉验收记录

## 2026-07-11：目录与第 1 章首页

- 状态：**PASS**
- 原书对照：PDF 第 17 页，使用 `pdftoppm -r 144` 渲染后人工核对。
- 桌面目录：1440 × 1000；目录结构完整，21 个配置条目全部出现。
- 桌面样章：1440 × 1200；标题、段落、1.1 节和状态说明均正常。
- 移动端样章：390 × 844；无页面级横向溢出，正文无需横向滚动。
- 浏览器控制台：0 个错误。
- 自动检查：`scrollWidth == clientWidth`，桌面为 1440，移动端为 390。

本记录只覆盖 `chapter-01` 的 PDF 第 17 页。后续每增加一批页面，都需要追加新的验收记录。

## 2026-07-11：第 1 章 PDF 第 18–20 页

- 状态：**PASS**
- 原书对照：PDF 第 18–20 页，使用 `pdftoppm -r 132` 渲染后逐页核对。
- 图 1.1：没有复用带英文标签的截图；已重建为中文、文字可选择的内联 SVG。
- 桌面样章：1440 × 1000；决策树完整显示，图容器 `scrollWidth == clientWidth == 958`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，宽图只在自身容器滚动。
- 移动端图形：图容器可从 `scrollLeft = 0` 滚动至 `596`，左右两端内容均可读取，并显示横向滚动提示。
- 尾注：尾注 1–4 在桌面与移动端均无裁切，正文引用均能跳到对应条目。
- 浏览器控制台：0 个错误；HTML ID 无重复，内部链接无断链。

本轮完成后，`chapter-01` 的 PDF 第 17–20 页均达到页面级 `verified`；章节本身仍为 `in_progress`。

## 2026-07-11：第 1 章 PDF 第 21–23 页

- 状态：**PASS**
- 原书对照：PDF 第 21–23 页，使用 `pdftoppm -r 132` 渲染后逐页核对。
- 结构：1 个三级小节、2 个“再思考”侧栏、图 1.2 与尾注 5–11 均已进入语义 HTML。
- 图 1.2：重建为中文内联 SVG；H、P、M 下标和四条过程模型到统计模型的交叉关系与原页一致。
- 桌面样章：1440 × 1000；图容器 `scrollWidth == clientWidth == 958`，整图直接可见。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，图容器可从 `scrollLeft = 0` 滚动至 `436`。
- 侧栏与尾注：2 个侧栏、11 条尾注均无裁切；正文引用均能跳到对应尾注。
- 浏览器控制台：0 个错误；HTML ID 无重复，内部链接无断链。

本轮完成后，`chapter-01` 的 PDF 第 17–23 页均达到页面级 `verified`；章节本身仍为 `in_progress`。

## 2026-07-11：第 1 章 PDF 第 24–26 页

- 状态：**PASS**
- 原书对照：PDF 第 24–26 页，使用 `pdftoppm -r 132` 渲染后逐页核对。
- 结构：1.2.2、两个四级小节、1.2.3、1.3 与 1.3.1 均按语义层级组织，没有保留 PDF 分页断句。
- 假说：4 个 H₀ 陈述使用独立语义块，桌面和移动端的下标、居中与边界均正常。
- 工具清单：4 个条目完整，无项目符号错位或文本裁切。
- 尾注：尾注 12–20 已加入；实验分析和数学哲学长链接在 390 px 视口正常换行。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。
- 浏览器控制台：0 个错误；4 个假说块、2 个四级标题、20 条尾注、4 个工具项均存在；HTML ID 无重复。

本轮完成后，`chapter-01` 的 PDF 第 17–26 页均达到页面级 `verified`；章节本身仍为 `in_progress`。

## 2026-07-12：第 1 章 PDF 第 27–29 页

- 状态：**PASS**
- 原书对照：PDF 第 27–29 页，使用 `pdftoppm -r 132` 渲染后逐页核对。
- 图 1.3：通过 `pdfimages -f 28 -l 28 -png` 从 PDF 原始嵌入对象提取，尺寸 `635 × 310`；发布资产与提取文件 SHA-256 均为 `056499d5b31b271dc6df78a32769bc702e9c1844a2a7d2ddaa3aa0a80743c050`。
- 媒体：浏览器报告图片加载完成且自然尺寸为 `635 × 310`；静态审计会打开并验证媒体文件，拒绝不可解码或小于 `80 × 80` 的图像。
- 结构：新增 1.3.2、两个“再思考”侧栏、两段语义引文和尾注 21–29。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，图像按比例缩放。
- 浏览器控制台：0 个错误；1 张图片、4 个累计侧栏、2 段引文、29 条尾注均存在；HTML ID 无重复。

本轮完成后，`chapter-01` 的 PDF 第 17–29 页均达到页面级 `verified`；章节本身仍为 `in_progress`。

## 2026-07-12：第 1 章 PDF 第 30–32 页

- 状态：**PASS**
- 原书对照：PDF 第 30–32 页，使用 `pdftoppm -r 132` 渲染后逐页核对。
- 结构：完成 1.3.2、1.3.3，并开始 1.3.4；新增尼安德特人和多层选举预测两个侧栏。
- 列表：交叉验证的 3 项用途与部分汇聚的 4 类应用均使用语义编号列表，桌面与移动端缩进清晰。
- 尾注：尾注 30–33 已加入，正文链接均能跳至对应条目。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。
- 浏览器控制台：0 个错误；累计 6 个侧栏、2 个用途列表共 7 项、33 条尾注、13 个章节区块均存在；HTML ID 无重复。

本轮完成后，`chapter-01` 的 PDF 第 17–32 页均达到页面级 `verified`；章节本身仍为 `in_progress`。

## 2026-07-12：第 1 章 PDF 第 33–34 页与全章验收

- 状态：**PASS**
- 原书对照：PDF 第 33–34 页，使用 `pdftoppm -r 132` 渲染后逐页核对；全章覆盖 PDF 第 17–34 页，共 18 页。
- 最后内容：完成 1.3.4、因果沙拉侧栏、1.4 总结、四部分路线图和尾注 34–36。
- 章节契约：`config/chapter-contracts.json` 要求 `verified` 状态、17–34 页、17 个关键 ID、14 个标题、14 个 `section`、3 幅图、2 个 SVG、1 张 PNG、7 个侧栏、2 段引文、4 个假说块、1 个工具清单、2 个用途列表、1 个路线图和 36 条尾注。
- 完整性：正文中有 36 个尾注引用，章末有 36 条尾注；所有引用目标存在，HTML ID 无重复。
- 图片：图 1.3 加载完成，自然尺寸 `635 × 310`；图 1.1–1.3 在桌面均为 960 px 宽，在移动端均为 358 px 宽，未越出视口。
- 桌面全章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，总内容高度 24,337 px。
- 移动端全章：390 × 844；页面 `scrollWidth == clientWidth == 390`，总内容高度 36,284 px。
- 锚点：为小节、图和尾注添加 `scroll-margin-top: 4.5rem`；`#sec-1-4` 与 `#fig-1-1` 在两个视口均停于 72 px，粘性顶栏底部为 51 px，不再遮挡标题。
- 浏览器控制台：0 个错误。

本轮完成后，`chapter-01` 的 PDF 第 17–34 页全部达到页面级 `verified`，章节状态升级为 `verified`。

## 2026-07-12：第 2 章 PDF 第 35–37 页

- 状态：**PASS**
- 原书对照：PDF 第 35–37 页，使用 `pdftoppm -r 132` 渲染后逐页核对。
- 图 2.1：通过 `pdfimages -f 36 -l 36 -png` 从 PDF 原始嵌入对象提取，尺寸 `1088 × 1083`；发布资产与提取文件 SHA-256 均为 `947450c8f96fd7393ecb96bf0aceb57c96e1d1db0b584092d5fe8bf6754d8062`。
- 弹珠猜想：5 种猜想使用带中文可访问标签的 HTML 弹珠组件；桌面为五列，390 px 视口重排为单列。
- 分叉路径：两幅教学图重建为含 `title`/`desc` 的 SVG；一次抽取图包含 4 条路径，两次抽取图包含 16 条路径。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，宽分叉图 `scrollWidth == clientWidth == 734`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`；宽分叉图容器为 368 px，可滚动至 `scrollLeft = 328`。
- 完整性：1 张图片加载完成，自然尺寸 `1088 × 1083`；5 种猜想、2 幅 SVG、4 个尾注引用和 4 条尾注均存在；HTML ID 无重复。
- 浏览器控制台：0 个错误。

本轮完成后，`chapter-02` 的 PDF 第 35–37 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 38–40 页

- 状态：**PASS**
- 原书对照：PDF 第 38–40 页，使用 `pdftoppm -r 132` 渲染后逐页核对；译文在第 40 页最后一个完整句子处收束，不跨页臆补断句。
- 图 2.2–2.4：由 `scripts/generate_marble_gardens.py` 确定性生成带 `title`、`desc` 和白色底板的 SVG；图 2.2 与图 2.3 各含 84 条连线和 85 个节点，图 2.4 含 254 条连线和 255 个节点。
- 媒体尺寸：浏览器报告图 2.2、图 2.3 的自然尺寸均为 `1000 × 470`，图 2.4 为 `1190 × 455`；三图在桌面端均完整显示。
- 计数表：五种猜想的相容路径数依次为 0、3、8、9、0；桌面与移动端均无裁切。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，三个图容器均为 `scrollWidth == clientWidth == 958`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`；三个宽图容器为 356 px，可滚动至 `scrollLeft = 596`，并显示横向滚动提示。
- 完整性：累计 6 个尾注引用和 6 条尾注均存在；HTML ID 无重复。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–40 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 41–43 页

- 状态：**PASS**
- 原书对照：PDF 第 41–43 页，使用 `pdftoppm -r 132` 渲染后逐页核对；覆盖 2.1.2、2.1.3 和第 40 页“论证依据”侧栏的跨页结尾。
- 结构：新增 2 个三级小节、2 张更新计数表、1 张概率表、3 个公式块、1 个“原初无知”侧栏、R 代码 2.1 和 4 个核心术语定义。
- 数值：第一张更新表的新计数为 0、3、16、27、0；工厂信息更新后的计数为 0、9、32、27、0；标准化可信程度为 0、0.15、0.40、0.45、0。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。三张表的容器均为 736 px，表格为 608 px；三条公式均无内部溢出，R 代码块 `scrollWidth == clientWidth == 734`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。三张表的容器为 370 px、表格为 608 px，可滚动至 `scrollLeft = 238`；左端猜想和右端新计数均已目视确认，并显示横向滚动提示。
- 公式与代码：移动端三条公式的 `scrollWidth == clientWidth == 370`；R 代码块和两段 `pre` 均为 `scrollWidth == clientWidth == 368`，没有裁切。
- 完整性：阶段契约锁定 PDF 第 35–43 页、10 个核心 ID、6 个关键标题和组件数量；累计 6 个尾注引用与 6 条尾注均存在，HTML ID 无重复。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–43 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 44–46 页

- 状态：**PASS**
- 原书对照：PDF 第 44–46 页，使用 `pdftoppm -r 132` 渲染后逐页核对；第 46 页在“观测到 W 时峰值向右移动”这一完整句后收束，不跨页补写下一句。
- 结构：完成 2.1.3 的跨页结尾，新增 2.2、2.2.1、2.2.2、2 个“再思考”侧栏、3 步模型设计循环、3 条数据故事、W/L 九次观测序列和尾注 43。
- 图 2.5：`scripts/generate_globe_learning.py` 按均匀先验与 `WLWWWLWLW` 序列确定性生成中文 SVG；包含 9 个更新面板和 18 条 Beta 曲线（每步各一条更新前虚线与更新后实线）。
- 媒体：SVG 声明 `1200 × 1040` 天然尺寸、`viewBox`、`title` 和 `desc`；自动审计现要求所有外部 SVG 同时声明宽高，避免浏览器回退到默认 300 px。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。图 2.5 渲染为 `928 × 804`，图容器 `scrollWidth == clientWidth == 958`，九个面板与图例完整显示。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。图容器为 356 px、内部宽度 952 px，可滚动至 `scrollLeft = 596`；左列 `n = 1/4/7` 与右列 `n = 3/6/9` 均已目视确认。
- 完整性：3 个新增章节锚点、9 个观测字符、3 个设计步骤、3 条数据故事、累计 5 个侧栏、7 个尾注引用和 7 条尾注均存在；HTML ID 无重复。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–46 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 47–49 页

- 状态：**PASS**
- 原书对照：PDF 第 47–49 页，使用 `pdftoppm -r 132` 渲染后逐页核对；完成图 2.5 的跨页续文，并在第 49 页二项分布公式的完整解释后收束，不跨页补写第 50 页 R 代码。
- 结构：新增 2.2.3、2.3、2.3.1、2.3.2、2.3.2.1，2 个“再思考”侧栏、3 项模型组成列表、1 个二项分布公式块、1 个公式读法块和尾注 44–46。
- 公式：语义 HTML 表示 `Pr(W, L | p) = ((W + L)! / (W! L!)) p^W (1 - p)^L`，并提供完整中文 `aria-label`；没有把公式做成图片。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。二项分布公式为 `672 × 98`，`scrollWidth == clientWidth == 672`；公式读法块内部宽度 605 px，无溢出。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。公式为 `370 × 98`，`scrollWidth == clientWidth == 370`；读法块内部宽度 367 px，三项组成列表内部宽度 366 px，均无裁切。
- 完整性：阶段契约覆盖 PDF 第 35–49 页、13 个章节区块、6 幅图、4 个公式块、7 个侧栏、10 个尾注引用和 10 条尾注；HTML ID 无重复。
- 锚点：手机端 `#note-46` 停在视口 515–613 px 范围内，全文与返回链接完整显示。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–49 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 50–52 页

- 状态：**PASS**
- 原书对照：PDF 第 50–52 页，使用 `pdftoppm -r 132` 渲染后逐页核对；覆盖 R 代码 2.2、2.3.2.2、2.3.3 与 2.4 的完整开头段落，没有跨到第 53 页。
- 结构：新增 1 个 R 代码块、2 个“过度思考”框、3 个“再思考”框、4 项参数问题、1 条均匀先验公式、2 条模型记法和尾注 47–48。
- 代码：R 代码 2.2 为 `dbinom( 6 , size=9 , prob=0.5 )`，输出 `[1] 0.1640625`；桌面与移动端代码及输出均完整。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。代码块 `scrollWidth == clientWidth == 734`；均匀先验公式为 `672 × 98`；两条模型记法均为 `496 × 69`，全部无内部溢出。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。代码块和两段 `pre` 均为 368 px；先验公式为 `333 × 98`；两条模型记法均为 `370 × 69`；参数问题列表内部宽度 368 px，均无裁切。
- 锚点：新增 `#eq-2-prior-uniform`、`#model-2-binomial`、`#model-2-prior`；公式块获得 `scroll-margin-top: 4.5rem`。手机端前两个锚点均停于 72 px，粘性顶栏底部为 51 px。
- 完整性：阶段契约覆盖 PDF 第 35–52 页、16 个章节区块、7 幅图、7 个公式块、2 个代码块、10 个“再思考”框、2 个“过度思考”框、12 个尾注引用和 12 条尾注；HTML ID 无重复。
- 尾注：手机端 `#note-48` 位于视口 565–614 px，全文与返回链接完整显示。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–52 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 53–55 页

- 状态：**PASS**
- 原书对照：PDF 第 53–55 页，使用 `pdftoppm -r 132` 渲染后逐页核对；覆盖完整的 2.4.1、2.4.2 和两个跨页连续段落，没有越过第 55 页结尾。
- 结构：新增 6 个语义公式块、图 2.6、3 项条件化引擎列表和 2 个“再思考”侧栏；没有新增尾注。
- 图 2.6：`scripts/generate_bayes_products.py` 确定性生成中文 SVG；3 行 × 3 列展示平坦、阶梯、尖峰先验与同一似然相乘所得的后验，共 9 条曲线。SVG 声明 `1160 × 890`、`viewBox`、`title` 和 `desc`。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。3 个简单等式均为 `672 × 66`，贝叶斯定理与文字公式均为 `672 × 98`，积分公式为 `672 × 65`；全部无内部溢出。
- 桌面图形：图 2.6 自然尺寸 `1160 × 890`，渲染为 `928 × 712`，图容器 `scrollWidth == clientWidth == 958`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。简单等式均为 `370 × 66`，贝叶斯定理与文字公式均为 `370 × 134`，积分公式为 `370 × 65`；条件化引擎列表内部宽度 366 px。
- 移动端图形：图容器为 356 px、内部宽度 952 px，可滚动至 `scrollLeft = 596`；左端三种先验和右端三个后验均已目视确认，并显示横向滚动提示。
- 锚点：`#eq-2-bayes-theorem` 在桌面停于 72–171 px，手机端停于 72–206 px，粘性顶栏底部为 51 px。
- 完整性：阶段契约覆盖 PDF 第 35–55 页、18 个章节区块、8 幅图、13 个公式块、12 个侧栏、12 个尾注引用和 12 条尾注；HTML ID 无重复。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–55 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 56–58 页

- 状态：**PASS**
- 原书对照：PDF 第 56–58 页，使用 `pdftoppm -r 132` 渲染后逐页核对；覆盖完整的 R 代码 2.3–2.5、图 2.7、2.4.3 与 2.4.4 方法说明，并在跨页未完的 R 代码 2.6 之前收束。
- 结构：新增 2 个章节区块、3 个代码块、图 2.7、5 步网格近似算法、1 个“向量化”过度思考框和 2 步二次近似算法；没有新增尾注。
- 编辑校正：原书第 57 页把复现三种先验的内部引用写成图 2.5；译文明确校正为图 2.6，并保留“原书此处误作图 2.5”的说明。
- 图 2.7：`scripts/generate_grid_approximation.py` 确定性生成中文 SVG；5 点与 20 点面板共含 25 个网格点和 2 条连线。SVG 声明 `1080 × 500`、`viewBox`、`title` 和 `desc`。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。三个代码块均为 734 px；图 2.7 自然尺寸 `1080 × 500`、渲染为 `928 × 430`，图容器 `scrollWidth == clientWidth == 958`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。三个代码块均为 368 px；代码 2.3、2.4 的 `pre` 分别可横向滚动 62 px、148 px，并显示滚动提示；代码 2.5 无需滚动，右端内容已目视确认。
- 移动端图形：图容器为 356 px、内部宽度 952 px，可滚动至 `scrollLeft = 596`；左侧 5 点近似与右侧 20 点近似均已目视确认。
- 列表：五步网格算法与两步二次近似列表内部宽度均为 368 px，无裁切。
- 完整性：阶段契约覆盖 PDF 第 35–58 页、20 个章节区块、12 幅图、5 个代码块、13 个公式块、12 个尾注引用和 12 条尾注；HTML ID 无重复。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–58 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 59–61 页

- 状态：**PASS**
- 原书对照：PDF 第 59–61 页，使用 `pdftoppm -r 132` 渲染后逐页核对；补全跨页的 R 代码 2.6，覆盖 `precis` 输出、R 代码 2.7、图 2.8、海森矩阵侧栏、完整的 2.4.5 和 R 代码 2.8。“蒙特卡洛地球仪投掷”侧栏解释将在 PDF 第 62 页继续。
- 结构：新增 3 个代码块、1 张五列表格、图 2.8、1 个章节区块、1 个“再思考”框和 2 个“过度思考”框；没有新增尾注。
- 图 2.8：`scripts/generate_quadratic_accuracy.py` 确定性生成中文 SVG；三个面板分别比较 `n = 9/18/36` 时的精确 Beta 后验与高斯二次近似，共 6 条曲线。SVG 声明 `1200 × 470`、`viewBox`、`title` 和 `desc`。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。代码 2.6、2.7 容器均为 734 px，代码 2.8 容器为 697 px；`precis` 表为 544 px。图 2.8 自然尺寸 `1200 × 470`、渲染为 `928 × 363`，图容器 `scrollWidth == clientWidth == 958`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`。代码 2.6、2.7 的 `pre` 分别可滚动 46 px、70 px；侧栏内代码 2.8 容器为 331 px，可滚动 115 px，最右端已目视确认。
- 移动端表格：`precis` 表宽 512 px，容器宽 370 px，可滚动 142 px；右侧 `94.5% = 0.92` 已目视确认，并显示表格滚动提示。
- 移动端图形：图容器为 356 px、内部宽度 952 px，可滚动至 `scrollLeft = 596`；左侧 `n = 9, w = 6` 与右侧 `n = 36, w = 24` 均已目视确认。
- 完整性：阶段契约覆盖 PDF 第 35–61 页、21 个章节区块、16 幅图、8 个代码块、13 个公式块、13 个“再思考”框、5 个“过度思考”框、12 个尾注引用和 12 条尾注；HTML ID 无重复。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–61 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 2 章 PDF 第 62–64 页与整章验收

- 状态：**PASS**
- 原书对照：PDF 第 62–64 页使用 `pdftoppm -r 132` 逐页渲染核对；补全 MCMC 侧栏和 R 代码 2.9，覆盖 2.5 总结以及 2.6 的全部练习。原书三页依次为 4 道简单题、7 道中等题和 4 道困难题，没有新增插图或尾注。
- 内容结构：新增 R 代码 2.9、2 个章节区块、3 个难度组和 15 张带稳定锚点的练习卡片；整章共 23 个章节区块、17 个 `figure`、9 个代码块、13 个公式块、13 个“再思考”框、5 个“过度思考”框和 12 条尾注。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。代码 2.9 容器宽 699 px，2.5 总结宽 736 px，简单题与困难题卡片均为 736 px；章节收束、练习层级与尾注过渡均已目视确认。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。15 张练习卡片均为 370 px，内部 `scrollWidth == clientWidth == 368`，无裁切或横向页面溢出。
- 移动端代码：代码 2.9 侧栏容器宽 333 px；`pre` 内容宽 422 px、可滚动 91 px，左端与右端 `add=TRUE` 均已目视确认，滚动提示可见。
- 移动端练习：含长条件概率表达式的 2E3、长段落的 2E4 和带项目符号列表的 2H4 均已目视确认；卡片边界、行距和难度分组清晰。
- 完整性：整章契约覆盖 PDF 第 35–64 页，要求全部章节锚点、代码、图、侧栏、练习和尾注数量；HTML ID 无重复，中文字符比例为 0.918。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-02` 的 PDF 第 35–64 页全部达到页面级 `verified`，章节状态提升为 `verified`。

## 2026-07-12：第 3 章 PDF 第 65–67 页

- 状态：**PASS**
- 原书对照：PDF 第 65–67 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖吸血鬼检测示例、3 组概率公式、R 代码 3.1、自然频数重述、后验样本的两项动机，以及跨至 PDF 第 68 页的“为什么统计学救不了糟糕的科学”侧栏。
- 尾注：从原书 PDF 第 585–598 页尾注区回溯并译入尾注 49–51；三个正文引用与回跳锚点均已验证，没有提前引入尾注 52。
- 结构：阶段契约覆盖 1 个代码清单、3 个公式块、2 个“再思考”框、1 组含 3 项的自然频数列表和 3 条尾注；HTML ID 无重复，中文字符比例为 0.890。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。三个公式块均为 672 px，代码 3.1 与两个侧栏均为 736 px；章节开篇、公式分式、代码输出与尾注过渡均已目视确认。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。三个公式块均为 370 px，频数列表、两则侧栏和尾注均无裁切或页面溢出。
- 移动端代码：代码 3.1 容器宽 370 px；代码内容宽 586 px、可滚动 218 px，左右两端及最终的 `Pr_Positive` 均已目视确认，滚动提示可见。
- 锚点修正：把 `aside[id]` 纳入 4.5 rem 的统一 `scroll-margin-top`，修复侧栏锚点跳转时标题被粘性顶栏遮挡的问题；两则侧栏在移动端复验通过。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–67 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 3 章 PDF 第 68–70 页

- 状态：**PASS**
- 原书对照：PDF 第 68–70 页使用 `pdftoppm -r 132` 逐页渲染核对；补完“为什么统计学救不了糟糕的科学”侧栏，覆盖 3.1、图 3.1、3.2、3.2.1 和 3.2.2 的引言，并在图 3.2 出现前收束。
- 内容结构：新增 1 个侧栏公式、7 个 R 代码清单（3.2–3.8）、4 个章节区块、1 组含 5 项的汇总问题、1 个“过度思考”框和尾注 52–53；阶段契约共锁定 5 个章节区块、9 个 `figure`、8 个代码块、4 个公式块、2 个“再思考”框、1 个“过度思考”框和 5 条尾注。
- 图 3.1：`scripts/generate_chapter3_samples.py` 使用固定种子确定性生成中文 SVG；左图包含按顺序绘制的全部 10,000 个网格后验样本，右图用同一批样本计算高斯核密度估计。SVG 为 `1200 × 520`，含 `viewBox`、`title`、`desc` 和 2 条路径；点云不透明度经网页缩放目视调整。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。8 个代码容器均为 736 px，侧栏双分式为 672 px；图 3.1 自然尺寸 `1200 × 520`、渲染为 `928 × 402`，图容器 `scrollWidth == clientWidth == 958`。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。侧栏双分式宽 328 px，无裁切；问题列表和 `sum` 计数侧栏均无页面溢出。
- 移动端代码：8 个代码容器均为 370 px；代码 3.2、3.3、3.6 分别可横向滚动 39、210、7 px，其余新增代码无需滚动。代码 3.3 左右两端及 `replace=TRUE` 已目视确认，滚动提示可见。
- 移动端图形：图容器宽 358 px、内部宽度 952 px，可滚动至 `scrollLeft = 596`；左侧 10,000 点样本云与右侧密度曲线均已目视确认。
- 完整性：阶段契约覆盖 PDF 第 65–70 页；HTML ID 无重复，中文字符比例为 0.847，尾注引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–70 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 3 章 PDF 第 71–73 页

- 状态：**PASS**
- 原书对照：PDF 第 71–73 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖图 3.2、R 代码 3.9–3.13、图 3.3、百分位区间与 HPDI 的比较，以及完整的“为什么是 95%？”侧栏；3.2.3 尚未开始。
- 内容结构：新增 5 个代码清单、2 幅多面板图、1 个“再思考”框、1 个 Fisher 引用块和尾注 54–56；阶段契约现锁定 16 个 `figure`、13 个代码块、4 个公式块、3 个“再思考”框、1 个“过度思考”框和 8 条尾注。
- 图 3.2：`scripts/generate_chapter3_intervals.py` 确定性生成 `1200 × 790` 中文 SVG；四个面板分别着色 `p < 0.5`、`0.5 < p < 0.75`、较低的 80% 与中间的 80%，曲线和边界均与原页核对。
- 图 3.3：同一脚本生成 `1200 × 470` 中文 SVG；左图着色 50% 百分位区间 `0.7037–0.9329`，右图着色 50% HPDI `0.8408–1.0`，清楚呈现后者更窄且包含最可能参数值 `p = 1`。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。13 个代码容器均为 736 px；图 3.2 自然尺寸 `1200 × 790`、渲染 `928 × 611`，图 3.3 自然尺寸 `1200 × 470`、渲染 `928 × 363`，两个图容器均为 960 px 且无内部溢出。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。新增代码中只有代码 3.11 需要横向滚动，内容宽 578 px、可滚动 210 px，右端 `replace=TRUE, prob=posterior` 已目视确认。
- 移动端图形：两图容器均宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`。图 3.2 左列/右列四面板与图 3.3 百分位/HPDI 两面板均逐一目视确认。
- 移动端侧栏：“为什么是 95%？”侧栏宽 370 px、高 858 px，Fisher 引用块宽 322 px；标题、引用、尾注链接与末段均无裁切或横向溢出。
- 完整性：阶段契约覆盖 PDF 第 65–73 页；HTML ID 无重复，中文字符比例为 0.835，尾注引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–73 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 3 章 PDF 第 74–76 页

- 状态：**PASS**
- 原书对照：PDF 第 74–76 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖 PI/HPDI 选择的收束、“兼容区间意味着什么？”侧栏、完整的 3.2.3、R 代码 3.14–3.19、图 3.4 和绝对距离损失示例。
- 内容结构：新增 1 个章节区块、6 个代码清单、1 幅双面板图、1 个“再思考”框和尾注 57–59；阶段契约现锁定 6 个章节区块、23 个 `figure`、19 个代码块、4 个公式块、4 个“再思考”框、1 个“过度思考”框和 11 条尾注。
- 图 3.4：`scripts/generate_chapter3_point_estimates.py` 确定性生成 `1200 × 470` 中文 SVG。左图绘制 Beta(4,1) 后验，并在 `0.8006/0.8408/0.9985` 标出均值、中位数和众数；右图对绝对距离损失进行后验数值积分，在中位数附近标出最小期望损失。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。19 个代码容器均为 736 px；兼容区间侧栏为 736 × 348 px；图 3.4 自然尺寸 `1200 × 470`、渲染 `928 × 363`，图容器为 960 px 且无内部溢出。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。新增代码中只有代码 3.18 需要横向滚动，内容宽 602 px、可滚动 234 px，左右两端与全部闭合括号均已目视确认。
- 移动端图形：图容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；左侧三种点估计与右侧期望损失最小点均已目视确认。
- 移动端侧栏：兼容区间侧栏宽 370 px、高 559 px；标题、两段正文、尾注 58 链接和 3.2.3 过渡无裁切或横向溢出。
- 完整性：阶段契约覆盖 PDF 第 65–76 页；HTML ID 无重复，中文字符比例为 0.845，尾注引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–76 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 3 章 PDF 第 77–79 页

- 状态：**PASS**
- 原书对照：PDF 第 77–79 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖点估计与损失函数的收束、3.3 的五类模拟用途、完整的 3.3.1、二项似然公式、R 代码 3.20–3.24 和图 3.5。
- 内容结构：新增 2 个章节区块、5 个代码清单、1 个公式块、1 组含 5 项的模拟用途列表、1 幅直方图和尾注 60–61；阶段契约现锁定 8 个章节区块、29 个 `figure`、24 个代码块、5 个公式块和 13 条尾注。
- 图 3.5：`scripts/generate_chapter3_dummy_histogram.py` 按 `Binomial(9, 0.7)` 确定性生成十万次模拟的期望频数；中文 SVG 为 `900 × 500`，0–9 次水域柱形完整，6–7 次为并列峰值。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。模拟用途列表为 736 × 436 px；二项公式为 672 × 98 px；图 3.5 自然尺寸 `900 × 500`、渲染 `720 × 400`，图容器为 960 px 且无内部溢出。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。模拟用途列表宽 370 px、高 615 px；二项公式宽 370 px，并自然换为两行，无裁切。
- 移动端代码：新增代码容器均为 370 px；代码 3.23、3.24 分别可横向滚动 7 px、39 px，其余无需滚动。代码 3.24 左右两端及 `xlab="dummy water count"` 已目视确认。
- 移动端图形：图容器宽 358 px、内部宽 744 px，可滚动至 `scrollLeft = 388`；左侧 0–4 次与右侧 5–9 次水域柱形均已目视确认。
- 完整性：阶段契约覆盖 PDF 第 65–79 页；HTML ID 无重复，中文字符比例为 0.853，尾注引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–79 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 3 章 PDF 第 80–82 页

- 状态：**PASS**
- 原书对照：PDF 第 80–82 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖“抽样分布”侧栏、完整的 3.3.2 与 3.3.2.1、3.3.2.2 的前半部分、观测不确定性与参数不确定性、后验预测分布、图 3.6，以及 R 代码 3.25–3.26。原页第 82 页末段在第 83 页续接，译稿已明确标注。
- 内容结构：新增 3 个章节区块、2 个代码清单、1 个“再思考”框和 1 幅三层示意图；本批没有新增尾注。阶段契约现锁定 11 个章节区块、32 个 `figure`、26 个代码块、5 个公式块、5 个“再思考”框、1 个“过度思考”框和 13 条尾注。
- 图 3.6：`scripts/generate_chapter3_posterior_predictive.py` 确定性生成 `1200 × 820` 中文 SVG；顶部为 Beta(7,4) 后验和 9 个示例参数值，中部为对应的 Binomial(9,p) 抽样分布，底部为按后验概率加权合成的 Beta-二项后验预测分布。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。“抽样分布”侧栏为 736 × 186 px；3.3.2.1 区块为 736 × 337 px；图 3.6 自然尺寸 `1200 × 820`、渲染为 `928 × 634`，图容器为 960 px 且无内部溢出。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。“抽样分布”侧栏宽 368 px、高 316 px；新增两段代码容器均为 368 px 且无需横向滚动。
- 移动端图形：图容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；左侧低概率抽样分布、右侧高概率抽样分布以及底部后验预测分布均已目视确认。
- 完整性：阶段契约覆盖 PDF 第 65–82 页；HTML ID 无重复，中文字符比例为 0.868，尾注引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–82 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 3 章 PDF 第 83–85 页

- 状态：**PASS**
- 原书对照：PDF 第 83–85 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖后验预测检查的序列相关性诊断、图 3.7、“更极端”侧栏、3.4 总结、3.5 练习的简单题与中等题 3M1–3M5、R 代码 3.27，以及尾注 62–63。
- 内容结构：新增 2 个章节区块、1 个代码清单、1 个“再思考”框、1 幅双面板图、2 个练习分组、12 道练习和 2 条尾注；阶段契约现锁定 13 个章节区块、34 个 `figure`、27 个代码块、5 个公式块、6 个“再思考”框、1 个“过度思考”框和 15 条尾注。
- 图 3.7：`scripts/generate_chapter3_predictive_checks.py` 确定性枚举全部 512 种九投掷序列，并按 Beta(7,4) 后验预测概率精确加权；左图汇总最长连续段，右图汇总切换次数，实际观测值 3 和 6 均以蓝色突出。中文 SVG 为 `1200 × 570`。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。图 3.7 自然尺寸 `1200 × 570`、渲染为 `928 × 441`，图容器为 960 px 且无内部溢出；“更极端”侧栏为 734 × 385 px；代码 3.27 为 734 × 193 px 且无需滚动。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。“更极端”侧栏宽 368 px、高 676 px；总结区块宽 370 px、高 481 px；练习区块宽 370 px、高 2379 px，首尾练习卡片均无裁切。
- 移动端图形：图容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；最长连续段和切换次数两个面板的坐标、柱形及蓝色观测标记均已目视确认。
- 移动端代码：代码 3.27 容器宽 368 px、内容宽 578 px，可滚动至 `scrollLeft = 210`；首尾字符和 `replace=TRUE` 均已目视确认。
- 完整性：阶段契约覆盖 PDF 第 65–85 页；HTML ID 无重复，中文字符比例为 0.873，15 条尾注的引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–85 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 3 章 PDF 第 86–88 页与整章验收

- 状态：**PASS**
- 原书对照：PDF 第 86–88 页使用 `pdftoppm -r 132` 逐页渲染核对；第 86 页覆盖 3M6、困难题说明、完整的两组 100 项出生数据、R 代码 3.28–3.30 和 3H1–3H4；第 87 页为 3H5；第 88 页经图像与文本层双重确认是空白保留页。
- 数据完整性：`birth1` 与 `birth2` 均为 100 项；男孩计数分别是 51 和 60，总计 111，与代码 3.30 的原书输出完全一致。
- 内容结构：新增 3 个代码清单、1 个练习分组和 6 道练习；整章契约锁定 PDF 第 65–88 页、13 个章节区块、37 个 `figure`、30 个代码块、5 个公式块、6 个“再思考”框、1 个“过度思考”框、18 道练习和 15 条尾注。
- 桌面整章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。代码 3.28 为 734 × 217 px，代码 3.29 为 734 × 75 px，代码 3.30 为 734 × 51 px，均无需横向滚动；3H5 卡片为 734 × 215 px。
- 移动端整章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。代码 3.28 容器宽 368 px、内容宽 531 px，可滚动至 `scrollLeft = 163`，两组向量的起止位置均已目视确认；代码 3.29 与 3.30 无需滚动。
- 移动端练习：3M6、3H1、3H3 和 3H5 卡片宽度均为 368 px，高度分别为 182、153、324 和 324 px；段落、行内代码和末尾问句均无裁切。
- 完整性：HTML ID 无重复，中文字符比例为 0.873，全部 15 条尾注引用与回跳链接完整；进度状态与章节契约同时升格为 `verified`。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-03` 的 PDF 第 65–88 页达到整章级 `verified`；全书累计完成 3/17 章、72/617 个 PDF 页面。

## 2026-07-12：第 4 章 PDF 第 89–91 页

- 状态：**PASS**
- 原书对照：PDF 第 89–91 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖第 4 章开篇、托勒密地心模型与线性回归的类比、图 4.1、完整的 4.1、4.1.1 前半部分、图 4.2、R 代码 4.1，以及尾注 64。PDF 第 91 页末段在第 92 页续接，译稿已明确标注。
- 术语：历史天文学术语采用“本轮”（epicycle）、“均轮”（deferent）和“对应点”（equant）；同时新增地心模型、傅里叶级数、线性回归、高斯分布、随机游走和正态性等术语映射。
- 图 4.1：`scripts/generate_chapter4_ptolemaic.py` 确定性生成 `900 × 600` 中文 SVG；标出地球、行星、本轮、均轮、均轮中心与对应点，并以虚线与箭头表示复合圆周运动。
- 图 4.2：`scripts/generate_chapter4_random_walks.py` 使用固定随机种子确定性生成 `1200 × 850` 中文 SVG；顶部绘制 100 条十六步随机游走，底部绘制 4、8、16 步的 3,000 次经验密度，并在 16 步面板叠加理论正态密度。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。图 4.1 自然尺寸 `900 × 600`、渲染为 `672 × 448`；图 4.2 自然尺寸 `1200 × 850`、渲染为 `928 × 657`；两个图容器均为 960 px 且无内部溢出，代码 4.1 为 734 × 51 px。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.1 容器宽 358 px、内部宽 696 px，可滚动至 `scrollLeft = 340`；图 4.2 内部宽 952 px，可滚动至 `scrollLeft = 596`；左右端标签、三幅密度图与理论曲线均已目视确认。
- 移动端代码：代码 4.1 容器宽 368 px、内容宽 407 px，可滚动至 `scrollLeft = 39`；表达式首尾与闭合括号均已目视确认。
- 完整性：阶段契约覆盖 PDF 第 89–91 页；HTML ID 无重复，中文字符比例为 0.965，尾注 64 的引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-04` 的 PDF 第 89–91 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 92–94 页

- 状态：**PASS**
- 原书对照：PDF 第 92–94 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖 4.1.1 收束、完整的 4.1.2 与 4.1.3、4.1.4 和两个嵌套依据小节、R 代码 4.2–4.5、两个乘法近似公式、厚尾侧栏、高斯密度公式侧栏，以及尾注 65–68。PDF 第 94 页侧栏末句在第 95 页续接，译稿已明确标注。
- 内容结构：新增 5 个章节区块、4 个代码清单、3 个公式块、1 个“再思考”框、1 个“过度思考”框和 4 条尾注；阶段契约现锁定 8 个章节区块、7 个 `figure`、5 个代码块、3 个公式块和 5 条尾注。
- 术语：新增有限方差、基因座、比例增加、乘性交互、对数尺度、本体论依据、认识论依据、微观过程、信息论、厚尾、连续/离散分布、概率质量函数和概率密度函数等映射。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。四个新增代码容器宽 734 px 且均无需滚动；厚尾侧栏为 734 × 243 px；高斯公式侧栏为 736 × 547 px，公式块为 672 × 96 px。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。厚尾侧栏为 368 × 454 px；高斯公式侧栏为 370 × 871 px；公式块为 333 × 162 px，分式自然换行且无裁切。
- 移动端公式：两个乘法近似公式宽 370 px，高度分别为 64 与 91 px；长等式自然换行，没有页面级溢出。
- 移动端代码：代码 4.2 无需滚动；代码 4.3 和 4.4 内容宽均为 485 px；代码 4.5 内容宽 516 px，可滚动至 `scrollLeft = 148`。三段长代码的变量名、参数值与闭合括号均已目视或文本层确认。
- 完整性：阶段契约覆盖 PDF 第 89–94 页；HTML ID 无重复，中文字符比例为 0.932，尾注 64–68 的引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-04` 的 PDF 第 89–94 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 95–97 页

- 状态：**PASS**
- 原书对照：PDF 第 95–97 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖高斯分布侧栏收束、精度参数形式、完整的 4.2 与 4.2.1、模型语言通用配方、地球仪模型重述、从模型定义到贝叶斯定理侧栏、R 代码 4.6、4.3 开篇、4.3.1 前半部分、R 代码 4.7 和尾注 69。PDF 第 97 页末段在第 98 页续接，译稿已明确标注。
- 内容结构：新增 4 个章节区块、2 个代码清单、4 个公式块、1 个“过度思考”框和 1 条尾注；阶段契约现锁定 12 个章节区块、9 个 `figure`、7 个代码块、7 个公式块、2 个“过度思考”框和 6 条尾注。
- 模型语言：新增 3 步联合生成模型配方、五行通用模型定义、两行地球仪模型、逐行自然语言读法和后验分式；术语表同步加入可观测/不可观测变量、同方差性、随机关系、确定性定义、预测变量、数据框等映射。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。精度参数公式为 672 × 64 px；五行模型定义为 672 × 186 px；后验分式为 672 × 96 px；代码 4.6 和 4.7 分别为 697 × 122 px、734 × 98 px，均无需横向滚动。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。精度参数公式为 333 × 64 px；五行模型定义为 370 × 186 px；地球仪模型为 370 × 98 px；模型自然语言读法为 367 × 136 px，均无裁切。
- 移动端贝叶斯侧栏：侧栏为 370 × 661 px，后验分式为 333 × 131 px；分子和积分分母在容器内完整显示。代码 4.6 容器宽 331 px、内容宽 414 px，可滚动至 `scrollLeft = 83`；四行代码的首尾均已目视确认。
- 移动端数据：代码 4.7 为 368 × 98 px 且无需滚动；尾注 69 宽 346 px、高 98 px，长网址自然断行，末尾路径和回跳链接可见。
- 完整性：阶段契约覆盖 PDF 第 89–97 页；HTML ID 无重复，中文字符比例为 0.899，尾注 64–69 的引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-04` 的 PDF 第 89–97 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 98–100 页

- 状态：**PASS**
- 原书对照：PDF 第 98–100 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖 Howell 数据框续文、R 代码 4.8–4.11、`str` 与 `precis` 输出、数据框和索引侧栏、成人样本筛选、完整的 4.3.2 前半部分、身高似然、独立同分布侧栏、带先验的三行模型，以及尾注 70–71。4.3.2 在 PDF 第 101 页继续，译稿已明确标注。
- 内容结构：新增 1 个章节区块、4 个代码清单、3 个公式块、2 个“过度思考”框、1 个“再思考”框和 2 条尾注；阶段契约现锁定 13 个章节区块、13 个 `figure`、11 个代码块、10 个公式块、2 个“再思考”框、4 个“过度思考”框和 8 条尾注。
- 数据输出：代码 4.8 保留 544 × 4 数据结构及示例值；代码 4.9 保留均值、标准差、5.5%/94.5% 分位数和四行字符直方图。术语表同步加入命名列、个体案例、双中括号表示法、索引、独立同分布、心智投射谬误、可交换、联合先验等映射。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`。代码 4.8、4.9 输出分别为 734 × 146 px、734 × 170 px 且无需滚动；数据框与索引侧栏均为 736 × 254 px；i.i.d. 侧栏为 734 × 614 px；三行先验模型为 672 × 127 px。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。代码 4.8 输出内容宽 414 px；代码 4.9 输出内容宽 422 px，可滚动至 `scrollLeft = 54`；字符直方图左右端均已目视确认。代码 4.10、4.11 无需滚动。
- 移动端侧栏：数据框侧栏为 370 × 461 px，索引侧栏为 370 × 435 px，i.i.d. 侧栏为 368 × 1037 px；标题、行内代码、公式、尾注 70–71 链接和末段均无裁切。
- 移动端模型：身高似然公式为 370 × 64 px；i.i.d. 公式为 328 × 64 px；带标签的三行模型为 370 × 127 px，似然与两个先验标签对齐且完整显示。
- 完整性：阶段契约覆盖 PDF 第 89–100 页；HTML ID 无重复，中文字符比例为 0.884，尾注 64–71 的引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-04` 的 PDF 第 89–100 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 101–103 页

- 状态：**PASS**
- 原书对照：PDF 第 101–103 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖 4.3.2 后半部分、R 代码 4.12–4.15、先验预测模拟、图 4.3、负身高与异常高身高示例、“告别 epsilon”侧栏、从身高模型到贝叶斯定理侧栏、4.3.3 开篇、R 代码 4.16，以及尾注 72。R 代码 4.16 后的绘图说明在 PDF 第 104 页继续，译稿已明确标注。
- 内容结构：新增 1 个章节区块、5 个代码清单、2 个公式块、1 个“再思考”框、1 个“过度思考”框、1 幅四面板图和 1 条尾注；阶段契约现锁定 14 个章节区块、19 个 `figure`、16 个代码块、12 个公式块、3 个“再思考”框、5 个“过度思考”框和 9 条尾注。
- 图 4.3：`scripts/generate_chapter4_prior_predictive.py` 确定性生成 `1200 × 850` 中文 SVG；上排显示均值与标准差先验，下排比较合理先验和过宽均值先验的身高预测，并以解析积分标注约 4% 的负身高概率和约 18% 的 272 厘米以上概率。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.3 自然尺寸 `1200 × 850`、渲染为 `928 × 657`，图容器为 960 × 762 px 且无内部溢出；代码 4.12–4.16 容器宽 734 px，均无需横向滚动。
- 桌面侧栏：epsilon 侧栏为 734 × 327 px，公式块为 672 × 98 px；身高后验侧栏为 736 × 407 px，贝叶斯分式为 672 × 131 px；标题、分子、二重积分分母和说明段落均完整可读。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.3 容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；四个面板、0/272 厘米标线、坐标与中文标签均已目视确认。
- 移动端代码：代码 4.12–4.15 的最大横向滚动量依次为 54、31、54、54 px；代码 4.16 内容宽 547 px，可滚动至 `scrollLeft = 179`。五段代码的首尾、参数值、对数似然和缩放表达式均已目视或文本层确认。
- 移动端侧栏：epsilon 侧栏为 368 × 407 px，公式块为 328 × 98 px；身高后验侧栏为 370 × 590 px，贝叶斯分式为 333 × 185 px，分子与分母在容器内自然换行；尾注 72 为 346 × 367 px，无裁切。
- 完整性：阶段契约覆盖 PDF 第 89–103 页；HTML ID 无重复，中文字符比例为 0.858，尾注 64–72 的引用与回跳链接完整。
- 浏览器控制台：0 个错误或警告。

本轮完成后，`chapter-04` 的 PDF 第 89–103 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 104–106 页

- 状态：**PASS**
- 原书对照：PDF 第 104–106 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖 R 代码 4.17–4.26、图 4.4、完整的 4.3.4、边际后验密度与兼容区间、样本量和 σ 后验正态性侧栏、20 个身高的网格近似，以及 4.3.5 开篇。模型定义在 PDF 第 107 页继续，译稿已明确标注。
- 内容结构：新增 2 个章节区块、10 个代码清单、1 个“过度思考”框和 1 幅联合后验散点图；阶段契约现锁定 16 个章节区块、30 个 `figure`、26 个代码块、12 个公式块、3 个“再思考”框、6 个“过度思考”框和 9 条尾注。
- 图 4.4：`scripts/generate_chapter4_height_posterior.py` 使用固定随机种子确定性生成 `900 × 650` 中文 SVG；绘制 10,000 个 μ 与 σ 的联合后验样本，以透明点云密度表达参数组合的相对后验可信程度。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.4 自然尺寸 `900 × 650`、渲染为 `928 × 670`，图容器为 960 × 753 px 且无需内部滚动；坐标、中文标签、中心点云和图注均完整。
- 桌面代码与侧栏：代码 4.17–4.22 和 4.26 容器宽 734 px，侧栏内代码 4.23–4.25 宽 697 px，均无需横向滚动；代码 4.24 高 441 px。σ 后验侧栏为 736 × 1208 px，4.3.5 区块为 736 × 682 px，无裁切。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.4 容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；左右坐标端点、完整点云和坐标标签均已目视确认。
- 移动端代码：代码 4.17、4.18、4.19、4.20 的最大横向滚动量分别为 31、15、156、280 px；代码 4.21–4.23 无需滚动；侧栏内代码 4.24 内容宽 539 px，可滚动至 `scrollLeft = 208`，代码 4.25 与 4.26 无需滚动。代码 4.20、4.24 的首尾表达式均已目视或文本层确认。
- 移动端侧栏：σ 后验侧栏为 370 × 1544 px，4.3.5 区块为 370 × 875 px；段落、行内公式、四段代码和 PDF 第 107 页续接提示均完整可读。
- 完整性：阶段契约覆盖 PDF 第 89–106 页；HTML ID 无重复，中文字符比例为 0.817，尾注 64–72 的引用与回跳链接完整。

本轮完成后，`chapter-04` 的 PDF 第 89–106 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 107–109 页

- 状态：**PASS**
- 原书对照：PDF 第 107–109 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖模型定义与 R 公式语法对照、R 代码 4.27–4.34、`precis` 输出、89% 兼容区间、`quap` 初始值侧栏、信息性均值先验示例、完整的 4.3.5，以及 4.3.6 的多维高斯、方差-协方差矩阵、相关矩阵和抽样代码。代码 4.34 的输出在 PDF 第 110 页继续，译稿已明确标注。
- 原书勘误提示：PDF 第 107 页右栏把 μ 先验写为 `dnorm(156,10)`，紧接着的正式模型代码为前文一致的 `dnorm(178,20)`；译稿保留原页两组数值，并以醒目的原书数值说明避免读者误解。
- 内容结构：新增 1 个章节区块、8 个代码清单、5 组代码输出、1 个公式映射和 1 个“过度思考”框；阶段契约现锁定 17 个章节区块、38 个 `figure`、34 个代码块、13 个公式块、7 个“过度思考”框和 9 条尾注。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。公式映射为 672 × 152 px，每行两列各约 310 px；原书数值说明为 640 × 87 px。全部代码 4.27–4.34 及五组输出均无需横向滚动。
- 桌面侧栏与矩阵：`quap` 初始值侧栏为 736 × 512 px，4.3.6 区块为 736 × 1563 px；`precis`、方差-协方差矩阵、相关矩阵及五行后验样本均完整对齐，无裁切。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。公式映射为 370 × 239 px，三行均折为单列且每行高约 56 px；原书数值说明为 370 × 144 px，无溢出。
- 移动端代码：除侧栏内代码 4.30 外，代码 4.27–4.34 和全部输出均无需横向滚动。代码 4.30 内容宽 383 px、可滚动至 `scrollLeft = 52`，初始值定义及末行 `start=start` 的首尾均已目视确认。
- 移动端侧栏与矩阵：`quap` 初始值侧栏为 370 × 693 px，4.3.6 区块为 370 × 2019 px；方差向量、2 × 2 相关矩阵、五行后验样本和 PDF 第 110 页续接提示均完整可读。
- 完整性：阶段契约覆盖 PDF 第 89–109 页；HTML ID 无重复，中文字符比例为 0.806，尾注 64–72 的引用与回跳链接完整。

本轮完成后，`chapter-04` 的 PDF 第 89–109 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 110–112 页

- 状态：**PASS**
- 原书对照：PDF 第 110–112 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖代码 4.34 的末行输出、R 代码 4.35–4.37、`quap` 后验样本汇总、多元抽样内部机制侧栏、完整的 4.4 开篇、回归概念侧栏、4.4.1 线性模型策略、两个带标签模型、完整的 4.4.1.1，以及 4.4.1.2 前半部分和尾注 73。关于结果均值的两个问题在 PDF 第 113 页继续，译稿已明确标注。
- 内容结构：新增 4 个章节区块、3 个代码清单、1 组代码输出、3 个公式块、1 个“再思考”框、1 个“过度思考”框和 1 条尾注；阶段契约现锁定 21 个章节区块、41 个 `figure`、37 个代码块、16 个公式块、4 个“再思考”框、8 个“过度思考”框和 10 条尾注。
- 概念与术语：新增线性预测、结果变量、线性模型、向均值回归、收缩、关联强度、可加关系、学习目标、多元抽样和群体遗传学等术语；基本高斯模型与身高-体重线性模型分别保留 3 行和 5 行带标签定义。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。代码 4.34–4.37 均无需横向滚动；代码 4.35 输出为 734 × 123 px，多元抽样侧栏为 736 × 351 px，回归侧栏为 736 × 295 px。
- 桌面公式：基本高斯模型为 672 × 129 px，五行线性模型为 672 × 188 px，线性均值公式为 672 × 66 px；公式、下标、横线变量与右侧定义标签均对齐完整。4.4 与 4.4.1 区块分别为 736 × 2839 px、736 × 1795 px。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。基本模型、五行线性模型和线性均值公式宽度均为 370 px；五行模型每行的公式列约 258–283 px、标签列约 34–59 px，无裁切。
- 移动端代码：代码 4.34、4.35 输入与代码 4.37 无需滚动；代码 4.35 输出内容宽 383 px，可滚动至 `scrollLeft = 15`，两行字符直方图首尾完整；侧栏内代码 4.36 内容宽 492 px，可滚动至 `scrollLeft = 161`，`mu=coef(m4.1)` 与 `Sigma=vcov(m4.1)` 均已目视确认。
- 移动端侧栏：多元抽样侧栏为 370 × 481 px，回归侧栏为 370 × 559 px，4.4 与 4.4.1 区块分别为 370 × 3868 px、370 × 2330 px；尾注 73 为 346 × 49 px，引用、回跳和 PDF 第 113 页续接提示均完整可读。
- 完整性：阶段契约覆盖 PDF 第 89–112 页；HTML ID 无重复，中文字符比例为 0.811，尾注 64–73 的引用与回跳链接完整。

本轮完成后，`chapter-04` 的 PDF 第 89–112 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 113–115 页

- 状态：**PASS**
- 原书对照：PDF 第 113–115 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖截距与期望变化率的两项解释、线性模型并不天然侧栏、单位与回归模型侧栏、完整的 4.4.1.3、R 代码 4.38–4.41、图 4.5、对数正态斜率先验，以及“什么是正确的先验？”侧栏前半部分和尾注 74。该侧栏在 PDF 第 116 页继续，译稿已明确标注。
- 内容结构：新增 1 个章节区块、4 个代码清单、3 个公式块、2 个“再思考”框、1 个“过度思考”框、1 幅双面板图、1 组两项问题和 1 条尾注；阶段契约现锁定 22 个章节区块、46 个 `figure`、41 个代码块、19 个公式块、6 个“再思考”框、9 个“过度思考”框和 11 条尾注。
- 图 4.5：`scripts/generate_chapter4_height_weight_priors.py` 使用固定随机种子确定性生成 `1200 × 650` 中文 SVG；每个面板绘制 100 条 α、β 联合先验关系线，左图为 `β ∼ Normal(0,10)`，右图为 `log(β) ∼ Normal(0,1)`，并标出 0 与 272 厘米参考线。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.5 自然尺寸 `1200 × 650`、渲染为 `928 × 503`，图容器为 960 × 586 px 且无需内部滚动；左右面板、坐标、参考线和中文标注均完整。
- 桌面代码与侧栏：代码 4.38–4.41 容器宽 734 px、高度分别为 156、275、109、156 px，均无需横向滚动；两项参数问题为 734 × 190 px，非线性模型侧栏为 734 × 325 px，单位侧栏为 736 × 389 px，单位公式为 672 × 113 px，“正确先验”侧栏为 734 × 379 px。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.5 容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；两个面板、坐标端点、0/272 厘米参考线和“胚胎”标注均已目视确认。
- 移动端代码：代码 4.38、4.40、4.41 无需滚动；代码 4.39 内容宽 469 px，可滚动至 `scrollLeft = 101`，绘图范围、循环、`add=TRUE` 与透明颜色末行均已目视或文本层确认。
- 移动端侧栏：两项问题为 368 × 340 px，非线性模型侧栏为 368 × 457 px，单位侧栏为 370 × 572 px，单位公式为 333 × 140 px并自然换行；先验区块为 370 × 3976 px，“正确先验”侧栏为 368 × 643 px，尾注 74 为 346 × 49 px，均无裁切。
- 完整性：阶段契约覆盖 PDF 第 89–115 页；HTML ID 无重复，中文字符比例为 0.811，尾注 64–74 的引用与回跳链接完整。

本轮完成后，`chapter-04` 的 PDF 第 89–115 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 116–118 页

- 状态：**PASS**
- 原书对照：PDF 第 116–118 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖“正确先验”侧栏收束、先验预测与 p-hacking 侧栏、完整的 4.4.2、数学模型与 R 代码对照、R 代码 4.42–4.44、依赖参数的量都有后验侧栏、log/exp 侧栏、4.4.3、参数含义侧栏，以及 4.4.3.1 开篇和边际后验表。参数解释在 PDF 第 119 页继续，译稿已明确标注。
- 内容结构：新增 3 个章节区块、3 个代码清单、1 组代码输出、1 个公式映射、3 个“再思考”框、1 个“过度思考”框和 1 组四项绘图用途；阶段契约现锁定 25 个章节区块、49 个 `figure`、44 个代码块、20 个公式块、9 个“再思考”框、10 个“过度思考”框和 11 条尾注。
- 模型语法：五行数学模型与 R 代码映射明确展示 `=` 对应 R 中的 `<-`，并保留 `dnorm`、`dlnorm` 和 `dunif` 参数化；术语表同步加入 p-hacking、数据前知识、模型拟合引擎、拟合度量、数量级、后验预测、交互项、多项式、模型批评与修订等映射。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。五行模型映射为 672 × 233 px，每行两列各约 310 px；代码 4.42、4.43、4.44 分别为 734 × 512、697 × 275、734 × 208 px，全部无需横向滚动，边际后验输出为 734 × 123 px。
- 桌面侧栏：p-hacking 侧栏为 734 × 265 px，依赖参数量侧栏为 734 × 240 px，log/exp 侧栏为 736 × 582 px，参数含义侧栏为 734 × 265 px；4.4.3 区块为 736 × 1536 px，四项绘图用途为 732 × 199 px，无裁切。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。五行模型映射为 370 × 378 px，每行折为单列且高约 56 px；数学式、R 代码与赋值运算符均完整。
- 移动端代码：代码 4.42 内容宽 383 px，可滚动至 `scrollLeft = 15`；侧栏内代码 4.43 内容宽 407 px，可滚动至 `scrollLeft = 76`，`exp(log_b)`、`weight - xbar` 和末行 `data=d2` 均已目视或文本层确认。代码 4.44 及其 368 × 123 px 输出无需滚动。
- 移动端侧栏：p-hacking、依赖参数量、log/exp 和参数含义侧栏高度分别为 424、373、816、450 px；4.4.3 区块为 370 × 2056 px，四项绘图用途为 366 × 259 px，参数解释续接提示完整可读。
- 完整性：阶段契约覆盖 PDF 第 89–118 页；HTML ID 无重复，中文字符比例为 0.808，尾注 64–74 的引用与回跳链接完整。

本轮完成后，`chapter-04` 的 PDF 第 89–118 页达到页面级 `verified`；章节状态仍为 `in_progress`。

## 2026-07-12：第 4 章 PDF 第 119–121 页

- 状态：**PASS**
- 原书对照：PDF 第 119–121 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖 β 的含义、方差—协方差矩阵与中心化、完整的 4.4.3.2、R 代码 4.45–4.49、图 4.6、4.4.3.3，以及 4.4.3.4 开篇。图 4.7 位于 PDF 第 122 页，当前译稿只保留明确续接提示，未建立空链接。
- 内容结构：新增 3 个章节区块、5 个代码清单、2 组代码输出和 1 幅散点回归图；阶段契约现锁定 28 个章节区块、55 个 `figure`、6 张图片、49 个代码块、11 组代码输出、20 个公式块、9 个“再思考”框、10 个“过度思考”框和 11 条尾注。
- 图 4.6：`scripts/generate_chapter4_height_weight_fit.py` 使用固定随机种子确定性生成 `900 × 650` 中文 SVG；绘制 352 个成年人的体重—身高散点和黑色后验均值线，坐标、图例与后验参数标注均与正文一致。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.6 自然尺寸 `900 × 650`、渲染为 `720 × 520`，图容器为 960 × 580 px 且无需内部滚动；代码 4.45–4.49 容器高度分别为 208、180、279、299、370 px，均无需横向滚动，两组输出分别为 734 × 123 px、734 × 171 px。
- 桌面章节：4.4.3.2、4.4.3.3、4.4.3.4 区块分别为 736 × 1369、736 × 1933、736 × 149 px；标题层级、图注、代码标签和 PDF 第 122 页续接提示均完整。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.6 容器宽 358 px、内部宽 744 px，可滚动至 `scrollLeft = 388`；左右坐标端点、352 个散点、黑色后验均值线和图内标注均已目视确认。
- 移动端代码：代码 4.45、4.47 无需滚动；代码 4.46、4.48、4.49 的内容宽分别为 391、399、453 px，均可在各自 368 px 容器内横向滚动。代码 4.49 可滚动至 `scrollLeft = 85`，20 次抽样、原始数据绘图与透明回归线循环的首尾完整。
- 移动端章节：4.4.3.2、4.4.3.3、4.4.3.4 区块分别为 370 × 1606、370 × 2411、370 × 206 px；页面无全局横向溢出，容器内滚动不影响正文布局。
- 完整性：阶段契约覆盖 PDF 第 89–121 页；HTML ID 无重复，中文字符比例为 0.801，尾注 64–74 的引用与回跳链接保持完整。

本轮完成后，`chapter-04` 的 PDF 第 89–121 页达到页面级 `verified`；章节状态仍为 `in_progress`。全书累计完成 3/17 章、105/617 个 PDF 页面。

## 2026-07-12：第 4 章 PDF 第 122–124 页

- 状态：**PASS**
- 原书对照：PDF 第 122–124 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖图 4.7、4.4.3.4 的主要推导、R 代码 4.50–4.56、图 4.8、50 千克处均值后验与 89% 相容区间，以及 `link` 矩阵的构造和汇总。图 4.9 位于 PDF 第 125 页，当前译稿只保留明确续接提示，未建立空链接。
- 内容结构：新增 2 幅图、7 个代码清单、3 组代码输出和 1 个公式块；阶段契约现锁定 28 个章节区块、64 个 `figure`、8 张图片、56 个代码块、14 组代码输出、21 个公式块、9 个“再思考”框、10 个“过度思考”框和 11 条尾注。
- 图 4.7 与图 4.8：`scripts/generate_chapter4_regression_intervals.py` 使用固定随机种子确定性生成 `1200 × 1030` 与 `900 × 540` 中文 SVG。图 4.7 的四个面板分别绘制 N = 10、50、150、352 及各 20 条后验回归线；图 4.8 绘制体重 50 千克时平均身高的钟形后验密度。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.7 自然尺寸 `1200 × 1030`、渲染为 `928 × 797`，图容器为 960 × 879 px；图 4.8 自然尺寸 `900 × 540`、渲染为 `720 × 432`，图容器为 960 × 493 px；两图均无需内部滚动。
- 桌面代码：代码 4.50–4.56 宽 734 px，高度分别为 75、51、51、75、241、170、98 px，均无需横向滚动；代码 4.52–4.54 的三组输出高度分别为 76、52、52 px。均值公式为 672 × 68 px，符号与上划线完整。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.7 容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；图 4.8 容器宽 358 px、内部宽 744 px，可滚动至 `scrollLeft = 388`。四个样本量面板、坐标端点、回归线与密度曲线左右端均已目视确认。
- 移动端代码：代码 4.50、4.52、4.53、4.56 无需滚动；代码 4.51、4.54、4.55 的内容宽分别为 492、461、578 px，可在各自 368 px 容器内横向滚动。代码 4.55 可滚动至 `scrollLeft = 210`，透明颜色参数与末行右端完整；三组输出仅代码 4.53 有 7 px 的微小内部滚动。
- 移动端章节：4.4.3.3 与 4.4.3.4 区块分别为 370 × 3381、370 × 4122 px；均值公式为 370 × 68 px，正文和 PDF 第 125 页续接提示均无裁切。
- 完整性：阶段契约覆盖 PDF 第 89–124 页；HTML ID 无重复，中文字符比例为 0.791，尾注 64–74 的引用与回跳链接保持完整。

本轮完成后，`chapter-04` 的 PDF 第 89–124 页达到页面级 `verified`；章节状态仍为 `in_progress`。全书累计完成 3/17 章、108/617 个 PDF 页面。

## 2026-07-12：第 4 章 PDF 第 125–127 页

- 状态：**PASS**
- 原书对照：PDF 第 125–127 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖图 4.9、R 代码 4.57–4.61、后验预测与区间的三步法、“过度自信的置信区间”侧栏、“link 怎样工作”侧栏，以及 4.4.3.5 预测区间的开篇、观测模型和 `sim` 工作流。图 4.10 位于 PDF 第 128 页，当前译稿只保留明确续接提示，未建立空链接。
- 内容结构：新增 1 个章节区块、1 幅双面板图、5 个代码清单、1 组代码输出、1 个公式块、1 个“再思考”框、1 个“过度思考”框和 1 组三步预测配方；阶段契约现锁定 29 个章节区块、70 个 `figure`、9 张图片、61 个代码块、15 组代码输出、22 个公式块、10 个“再思考”框、11 个“过度思考”框和 11 条尾注。
- 图 4.9：`scripts/generate_chapter4_regression_intervals.py` 使用固定随机种子确定性生成 `1200 × 650` 中文 SVG；左图按 46 个体重值分别绘制前 100 个均值后验样本，右图绘制 352 个个体、黑色后验均值线及灰色 89% 相容区间。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.9 自然尺寸 `1200 × 650`、渲染为 `928 × 503`，图容器为 960 × 585 px 且无需内部滚动；左右面板、坐标端点、蓝色样本与灰色区间均完整。
- 桌面代码与侧栏：代码 4.57–4.61 高度分别为 241、170、75、51、288 px，均无需横向滚动；代码 4.59 输出为 734 × 52 px。三步配方为 736 × 227 px，“过度自信”侧栏为 736 × 174 px，`link` 侧栏为 736 × 509 px，观测模型公式为 672 × 68 px，4.4.3.5 区块为 736 × 1440 px。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.9 容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；后验样本面板与均值区间面板的左右端均已目视确认。
- 移动端代码：代码 4.57–4.61 内容宽分别为 508、508、469、446、469 px；外部代码容器宽 368 px，侧栏内代码 4.58 容器宽 331 px。代码 4.58、4.61 分别可滚动至 `scrollLeft = 177`、`101`，函数定义、`prob=0.89` 和末行右端完整；代码 4.59 输出无需滚动。
- 移动端侧栏与章节：三步配方为 370 × 346 px，“过度自信”侧栏为 370 × 253 px，`link` 侧栏为 370 × 639 px，观测模型公式为 370 × 68 px，4.4.3.5 区块为 370 × 1752 px；正文与 PDF 第 128 页续接提示均无裁切。
- 完整性：阶段契约覆盖 PDF 第 89–127 页；HTML ID 无重复，中文字符比例为 0.781，尾注 64–74 的引用与回跳链接保持完整。

本轮完成后，`chapter-04` 的 PDF 第 89–127 页达到页面级 `verified`；章节状态仍为 `in_progress`。全书累计完成 3/17 章、111/617 个 PDF 页面。

## 2026-07-12：第 4 章 PDF 第 128–130 页

- 状态：**PASS**
- 原书对照：PDF 第 128–130 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖图 4.10、R 代码 4.62–4.65、“两种不确定性”侧栏、“自己实现 sim”侧栏、4.5、4.5.1、完整数据结构输出、抛物线均值模型、标准化与六行数学/R 代码映射。代码 4.65 在 PDF 第 131 页继续，当前译稿保留明确续接提示。
- 内容结构：新增 2 个章节区块、1 幅图、4 个代码清单、1 组代码输出、2 个公式块、1 个“再思考”框和 1 个“过度思考”框；阶段契约现锁定 31 个章节区块、75 个 `figure`、10 张图片、65 个代码块、16 组代码输出、24 个公式块、11 个“再思考”框、12 个“过度思考”框和 11 条尾注。
- 图 4.10：`scripts/generate_chapter4_regression_intervals.py` 使用固定数据与确定性计算生成 `900 × 650` 中文 SVG；绘制 352 个个体、黑色平均身高线、深灰 <span class="math-inline">μ</span> 的 89% 区间及浅灰实际身高的 89% 后验预测区间。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.10 自然尺寸 `900 × 650`、渲染为 `720 × 520`，图容器为 960 × 625 px 且无需内部滚动；两层阴影、散点、坐标和图内说明均完整。
- 桌面代码与侧栏：代码 4.62–4.65 高度分别为 75、217、122、98 px，均无需横向滚动；代码 4.64 输出为 734 × 147 px。“两种不确定性”侧栏为 736 × 265 px，“自己实现 sim”侧栏为 736 × 468 px；4.5 与 4.5.1 区块分别为 736 × 2139、736 × 1785 px。
- 桌面模型：抛物线均值公式为 672 × 71 px，六行数学/R 代码映射为 672 × 306 px；左右两列对齐，<span class="math-inline">β₁</span>、<span class="math-inline">β₂</span>、平方项和全部先验完整可读。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.10 容器宽 358 px、内部宽 744 px，可滚动至 `scrollLeft = 388`；左右坐标端点、两层区间与黑色均值线均已目视确认。
- 移动端代码：代码 4.62–4.65 内容宽分别为 531、446、368、469 px；侧栏内代码 4.63 容器宽 331 px，可滚动至 `scrollLeft = 115`，代码 4.65 可滚动至 `scrollLeft = 101`。代码 4.64 输出内容宽 407 px，可在 368 px 容器内滚动；所有首尾内容可达。
- 移动端侧栏与模型：“两种不确定性”与“自己实现 sim”侧栏高度分别为 450、572 px；抛物线公式为 370 × 71 px，六行模型映射折为单列后为 370 × 460 px；4.5 与 4.5.1 区块分别为 370 × 2831、370 × 2357 px，无裁切。
- 完整性：阶段契约覆盖 PDF 第 89–130 页；HTML ID 无重复，中文字符比例为 0.778，尾注 64–74 的引用与回跳链接保持完整。

本轮完成后，`chapter-04` 的 PDF 第 89–130 页达到页面级 `verified`；章节状态仍为 `in_progress`。全书累计完成 3/17 章、114/617 个 PDF 页面。

## 2026-07-12：第 4 章 PDF 第 131–133 页

- 状态：**PASS**
- 原书对照：PDF 第 131–133 页使用 `pdftoppm -r 132` 逐页渲染核对；覆盖代码 4.65 的续页、R 代码 4.66–4.71、系数表、图 4.11、二次与三次回归比较、三次模型定义、“线性、可加、古怪”侧栏、“转换回自然尺度”侧栏及 4.5.2 样条开篇。B 样条说明在 PDF 第 134 页继续；尾注 75 已从原书尾注区提取并建立引用与回跳。
- 内容结构：新增 1 个章节区块、1 幅三面板图、6 个代码清单、1 组代码输出、1 个公式映射、1 个“再思考”框、1 个“过度思考”框和 1 条尾注；阶段契约现锁定 32 个章节区块、82 个 `figure`、11 张图片、71 个代码块、17 组代码输出、25 个公式块、12 个“再思考”框、13 个“过度思考”框和 12 条尾注。
- 图 4.11：`scripts/generate_chapter4_polynomial_regression.py` 使用固定随机种子确定性生成 `1200 × 650` 中文 SVG；三个面板分别拟合线性、二次与三次多项式，均绘制 544 个模拟完整样本、黑色均值曲线、深灰均值区间和浅灰预测区间。
- 桌面样章：1440 × 1000；页面 `scrollWidth == clientWidth == 1440`，正文宽 736 px。图 4.11 自然尺寸 `1200 × 650`、渲染为 `928 × 503`，图容器为 960 × 607 px 且无需内部滚动；三面板标题、坐标、曲线和两层区间完整。
- 桌面代码与模型：代码 4.65–4.71 高度分别为 312、51、193、122、312、51、98 px，均无需横向滚动；代码 4.66 输出为 734 × 147 px。三次模型数学/R 代码映射为 672 × 349 px，七行内容左右对齐。
- 桌面侧栏与章节：“线性、可加、古怪”侧栏为 736 × 244 px，“转换回自然尺度”侧栏为 736 × 508 px；4.5.1 区块为 736 × 5690 px，4.5.2 区块为 736 × 184 px；尾注 75 为 712 × 54 px。
- 移动端样章：390 × 844；页面 `scrollWidth == clientWidth == 390`，正文宽 370 px。图 4.11 容器宽 358 px、内部宽 952 px，可滚动至 `scrollLeft = 596`；左、中、右三个面板均已分别目视确认。
- 移动端代码：代码 4.65–4.71 内容宽分别为 469、368、531、477、508、563、399 px；外部代码容器宽 368 px，侧栏内代码 4.70/4.71 容器宽 331 px。代码 4.69 可滚动至 `scrollLeft = 140`，代码 4.70 可滚动至 `scrollLeft = 232`；全部首尾内容可达，代码 4.66 输出无需滚动。
- 移动端模型与侧栏：三次模型映射折为单列后为 370 × 556 px；两个侧栏高度分别为 403、611 px，4.5.1 与 4.5.2 区块分别为 370 × 7107、370 × 270 px；尾注 75 为 346 × 105 px，无裁切。
- 完整性：阶段契约覆盖 PDF 第 89–133 页；HTML ID 无重复，中文字符比例为 0.767，尾注 64–75 的引用与回跳链接完整。

本轮完成后，`chapter-04` 的 PDF 第 89–133 页达到页面级 `verified`；章节状态仍为 `in_progress`。全书累计完成 3/17 章、117/617 个 PDF 页面。
